/**
 * Script pour corriger la couleur du titre sur la page d'accueil de l'administration
 * Ce script applique dynamiquement la couleur du texte définie dans les variables CSS
 */
document.addEventListener('DOMContentLoaded', function() {
    // Neutralisé: laisser le CSS gérer la couleur des titres, pas d'injection inline
    function applyTextColor() { return; }
    // Pas d'application ni de ré-application
});
