/**
 * Script pour appliquer directement la couleur du texte sur la page d'accueil
 * Cette approche est plus directe et contourne les problèmes de spécificité CSS
 */
document.addEventListener('DOMContentLoaded', function() {
    // Neutralisé: ne plus appliquer de styles inline pour laisser le CSS gérer les couleurs
    function applyTextColorDirectly() {
        return; // noop
    }

    // Ne pas appliquer
    // setTimeout/appels périodiques supprimés intentionnellement
});
